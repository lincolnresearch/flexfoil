---
sidebar_position: 4
title: Web App
---

# Web App

FlexFoil includes a browser-based analysis environment. Use it at
[foil.flexcompute.com](https://foil.flexcompute.com/flexfoil/), or launch it
locally from Python:

```python
import flexfoil
flexfoil.serve()  # opens http://localhost:8420
```

```bash
flexfoil serve    # or from the command line
```

The web app runs the same RustFoil solver compiled to WebAssembly, so analysis
happens entirely in the browser with no server round-trips. When launched via
`flexfoil.serve()`, it shares the same SQLite database as the Python API — runs
appear in both places.

## Panels

The UI is organized as a set of dockable, rearrangeable panels.

### Airfoil Library

Browse and load airfoils from the NACA 4-digit generator, import `.dat` files,
or search the UIUC Selig database (~1,600 airfoils). Type at least two
characters in the Selig search box to filter by name or filename, then click an
entry to fetch and load it. The **Random Foil** button picks a random airfoil
from the database and loads it instantly.

### Airfoil Canvas

Interactive airfoil display with pan and zoom. Shows the current geometry with
panel nodes, and overlays pressure distributions, boundary layer thickness, and
other flow quantities when a solution is available.

### Solve

Configure and run single-point analysis. Set angle of attack, Reynolds number,
Mach number, and N_crit. Toggle between viscous and inviscid modes. Results
update in real time.

### Parameter Sweep

Sweep any solver or geometry parameter: alpha, Reynolds, Mach, Ncrit, flap
deflection, or flap hinge x/c. Optionally enable a **matrix sweep** with a
second parameter to generate a grid of polar series (e.g., alpha vs Reynolds,
or flap deflection vs Ncrit). Results plot as overlaid series in the Polar panel.

### Polar Plot

Visualize sweep results with configurable axes — CL, CD, CM, L/D, alpha,
Reynolds, Mach, Ncrit, flap deflection, and flap hinge x/c. Overlay
multiple series for direct comparison. Enable the **Outliers** checkbox to
automatically remove statistical outliers (IQR method) from the displayed
data. Right-click any data point to manually flag it as an outlier; flagged
points appear as red X marks and are excluded when the outlier filter is on.

### Visualization

Real-time flow visualization powered by WebGPU:

- **Streamlines** — potential flow streamlines around the airfoil
- **Smoke** — particle-based smoke visualization
- **Cp** — pressure coefficient distribution
- **Boundary layer** — displacement thickness envelope
- **Wake** — wake trajectory downstream of the trailing edge

### Data Explorer

A full-featured data table (AG Grid) of all solver runs in the database. Sort,
filter, and search across airfoils, operating conditions, and results. Includes
a SPLOM (scatter plot matrix) correlogram for multivariate exploration. Custom
computed columns let you define algebraic expressions over existing fields. The
correlogram view supports automatic outlier removal via the **Outliers** toggle.
Right-click a scatter point to manually flag it as an outlier; flagged runs are
persisted in the database and excluded from all plots. The table includes a
filterable **Outlier** column.

**Row grouping and aggregation** — drag any column header (e.g. Airfoil, Re) into
the row group panel above the grid. Grouped rows automatically aggregate their
children: CL shows `max`, CD shows `min`, L/D shows `max`, etc. Change the
aggregation function for any column via the sidebar (right-click a column header
→ "Value Aggregation"). Custom aggregation functions include:

- **at max(CL)** / **at min(CD)** / **at max(L/D)** — returns the value of the
  current column at the row where the target column reaches its extremum.
  For example, applying "at max(CL)" to the alpha column gives **α_stall**.
- **median** — median aggregation (not available by default in AG Grid).

Two predefined aerodynamic summary columns are available (hidden by default —
show them via the sidebar):
- **α_stall** — angle of attack at CL_max
- **α @ L/D_max** — angle of attack at maximum lift-to-drag ratio

Use the **Converged Only** button to quickly filter out non-converged solver
points before grouping, which is particularly useful near stall where the
solver may not converge reliably.

### Plot Builder

Build custom charts from run data: scatter, line, bar, and histogram plots.
Map any database field to axes, color, or size. Useful for parameter studies
and trade-off visualization. Toggle **Outliers** to automatically exclude
statistical outliers using the IQR method. Right-click any plotted point to
manually flag it as an outlier; flagged runs are persisted and excluded from
the chart.

The **Aggregated** data source (available when row grouping is active in the
Data Explorer) lets you plot group-level statistics directly. For example, to
plot L/D_max vs Reynolds number for multiple airfoils: group by Airfoil and Re
in the Data Explorer, then select Data → Aggregated, X → Re, Y → L/D in the
Plot Builder.

### Case Logs

View the solver's internal convergence log for the current case — Newton
iterations, residuals, and boundary layer events.

### Solver Queue (Status Bar)

The status indicator in the bottom-right corner of the window shows what the
solver is currently doing. Click it to expand the **Solver Queue** popover:

- **Running jobs** display a live progress bar and point counter (e.g.,
  *12/21 points*).
- **Cancel** any running sweep or polar directly from the queue.
- **Completed and failed** jobs remain in the history until cleared.

All sweep types — single-parameter sweeps, matrix sweeps, and legacy alpha
polars — register in the queue automatically. The inline indicator pulses
yellow while a job is active and shows a mini progress bar so you can track
long sweeps without switching panels.

## Design modes

### Geometry Design (GDES)

Direct geometry manipulation tools:

- **Flap deflection** — define one or more trailing-edge flaps with configurable hinge x/c, hinge y/t, and deflection angle
- **Trailing-edge gap** — modify the TE gap
- **Leading-edge radius** — scale the LE radius
- **Camber/thickness splines** — interactive control points for shape editing

### Inverse Design (QDES / MDES)

:::caution Experimental
QDES and MDES are works in progress and may produce unexpected results.
:::

Mixed-inverse design mode (QDES): prescribe a target pressure distribution (Cp or
velocity) on part of the airfoil surface, and the solver computes the geometry
that produces it. Full-inverse mode (MDES) designs an entire airfoil from a
velocity distribution via circle-plane conformal mapping. Both are useful for
designing airfoils to meet specific aerodynamic targets.

## Command palette

Press **⌘K** (Mac) or **Ctrl+K** (Windows/Linux) to open the command palette.
Type to fuzzy-search across panels, sub-features (e.g. "control surface" finds
Flap Design), actions (undo, toggle theme), and guided tours. You can also
click the **Search...** button in the menu bar.

## Sharing and persistence

- **URL sharing** — the full application state (airfoil, solver settings,
  panel layout, visualization) is encoded in the URL. Copy and share a link to
  reproduce exactly what you see.
- **SQLite database** — all runs are persisted locally. Export and import the
  database for backup or sharing.
- **Mobile layout** — the app adapts to smaller screens with a stacked panel
  layout.
